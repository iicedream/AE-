本项目实现将黑白照片处理成彩色照片。
可实现其基本功能，由于本人电脑配置一般，所以仅将照片全部初始化成128*128的大小，无论输入的照片是什么样的，只要是*.jpg放入/data/train中就可以直接训练。
可以实现每50个epoch保存一下训练效果，其图片为8*3张放在一个图中，保存路径为picture。
模型训练每隔100epoch保存一下模型，保存的位置为model。
测试时照片放入？data/test中，为了演示，这次仍然使用彩色照片，然后自动变成黑白照片，之后生成彩色照片和原来的作对比，得到的结果在score文件夹中。
训练和测试文件都放入了一小部分便于理解。
直接运行python main.py 即可。
有个问题，生成的照片偏黄，暂时还不知道什么原因，本次只是实现一个思路。

需要的环境pytorch 1.0以上，pandas和numpy版本一般都没问题。
